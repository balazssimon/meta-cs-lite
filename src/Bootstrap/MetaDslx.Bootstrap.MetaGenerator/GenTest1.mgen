namespace MetaDslx.Bootstrap.MetaGenerator;

using System;
using System.Linq;

generator GenTest1;

template SayHello(IEnumerable<string> names)
    hello
    [foreach (var name in names) separator ","]
        [name]
        [foreach (var name2 in names) separator ","]
            [name2]
        [end foreach]
    [end foreach]

    enum Colors
    {
        [foreach (var name in names) separator ","]
        [name]
        [end foreach]
    }
end template

